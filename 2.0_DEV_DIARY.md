# AI-Novel-Forge · 2.0 开发日记

> 创建：2026-08-12 ｜ 用途：2.0 迭代的「决策记录 + 开发计划 + 进度追踪」。
> 前置基线：`PROJECT_STATE.md`（v0.1 最新状态快照）。
> 设计规格来源：主提示词（AI 架构师视角）+ 7 个子提示词
> （PlotArchitect / WorldBuilder / ChapterWriter / ConflictEditor / MemoryKeeper / RAG / Coordinator）
> + 待编写的 CharacterDesigner（第 8 个，本日记 §4 共创中）。

---

## 0. 背景
- 项目 = 主提示词（全栈多 Agent 网文创作系统）的产物；7 个子提示词是用户在特定要求下让 AI 生成的「各 Agent 详细规格」。
- 现状（见 `PROJECT_STATE.md`）：代码里的 6 个 Agent 是子提示词的「简化版」，WorldBuilder / CharacterDesigner / MemoryKeeper 未接路由，Coordinator 架构完全不存在。
- 2.0 目标：把「协调器驱动的多 Agent 流水线」补完，并逐 Agent 对齐到设计规格。

---

## 1. 已确认的设计决策（2026-08-12）
1. **CharacterDesigner 规格缺失** → 采用「问答共创」方式：由 AI 提问、与用户共同编写（见 §4，进行中）。
2. **世界观存储**：落库 `novels.world_settings_json`（结构化 JSON），**可查看、可手动编辑**；同时同步写一份到 RAG `world` 集合供检索。
3. **角色状态**：采用**结构化字段**。在 `characters` 表新增 `level / mood / equipment / location`（保留 `current_status` 自由文本）。
4. **审校重写**：采用**完全自动**方案 —— ConflictEditor 评分不达标则自动打回 ChapterWriter 重写，**最多 2 次**，仍不达标则强制采用并标记「待人工复核」。
5. **大纲↔角色循环依赖**：采用「PlotArchitect 两遍式 + CharacterDesigner 居中」消解 —— Pass-1 大纲骨架（仅角色位占位）→ CharacterDesigner 出人设卡 → Pass-2 大纲细化（消费人设卡织入章节细纲，即「大纲跟着角色走」）。由 Coordinator 统一编排，仅一次有界细化，不无限循环。

---

## 2. 差距分析（子提示词规格 vs 当前代码）— 摘要
| Agent | 状态 | 关键差距 |
|---|---|---|
| PlotArchitect | 已接（简化） | 只传 `novel_id` 未注入设定；outline schema 缺 `total_outline` / `arc` / `development` / `ending_hook` |
| WorldBuilder | 未接（stub） | 仅简化 `entries[]`；需扩为完整世界结构 + endpoint + 写 RAG + 落库 |
| CharacterDesigner | 未接（stub） | 需编写完整子提示词 + endpoint + 写 `characters` 表（结构化字段） |
| ChapterWriter | 已接（简化） | 缺 开头/发展/爽点/收尾 结构、对话比、禁 AI 味、末尾元数据行 |
| ConflictEditor | 已接（简化） | 缺四维评分 / verdict / `{severity,location,fix}` / 重写循环 |
| MemoryKeeper | 未接（stub） | 缺 `expected_resolve` / `timeline` / `warnings`；需每章后触发 |
| RAG 检索器 | 已接（部分） | 无 RRF / 无 `context_pack` 结构 / 无 token 预算与检索日志 |
| Coordinator | 不存在 | 需新建编排层（创建新书 + 生成单章两条流水线 + 重试/重写 + 调用链日志） |

---

## 3. 分阶段开发计划（每阶段独立可验收）
- **Phase 0 清理**：删 `api/chapter.py` 死代码；修 PlotArchitect 注入 `title/genre/premise/style`；outline schema 补齐 `development` / `ending_hook` / `total_outline`。
  验收：`generate-outline` 产出含完整结构的 JSON。
- **Phase 1 WorldBuilder**：扩 schema + `/generate-world` + 写 RAG + 落 `world_settings_json` + WorldView 可查看/编辑。
  验收：世界观进入向量库且前端可改。
- **Phase 2 CharacterDesigner**：编写子提示词（§4）→ endpoint + 写 `characters` 表（结构化字段）+ 角色卡 UI。
  验收：人设卡可生成/查看/编辑。
- **Phase 3 写手/审校对齐**：升级 ChapterWriter / ConflictEditor 提示词 + 解析新 schema（四维评分 / verdict / 重写建议 / 元数据行）。
  验收：审校输出结构化、可驱动自动重写。
- **Phase 4 MemoryKeeper**：每章后触发，更新角色/伏笔/时间线，合并现有 `build_generation_context`。
  验收：角色状态/伏笔随章节自动演进。
- **Phase 5 Coordinator**：新建编排层 + 创建新书流水线 + 生成单章流水线（RAG→写→审→≤2 次重写→记忆→RAG 写入）+ 调用链日志。
  验收：一条 API 跑完整流水线。
- **Phase 6 体验层**：流式打字机输出 + 前端流程串联 + 重试/错误处理。
  验收：端到端可玩。

### 3.1 创建新书流水线（消解循环依赖，由 Coordinator 编排）
顺序（注意 PlotArchitect 跑两遍）：
1. 用户输入 premise（title/genre/premise/style）→ 存 `novels` 表
2. **WorldBuilder**：premise → 世界观 → 存 `world_settings_json` + 写 RAG `world`
3. **PlotArchitect Pass-1（骨架）**：premise + world → `outline_skeleton`（卷/章结构 + 角色位占位 + 核心冲突；此时角色是"角色位"不是人设卡）
4. **CharacterDesigner**：premise + world + outline_skeleton → 完整人设卡（含反派独立动机）→ 存 `characters` 表
5. **PlotArchitect Pass-2（细化）**：outline_skeleton + 人设卡 → `outline_json` 终稿（把角色动机/弧光织入具体章节；补 `development`/`ending_hook`/`total_outline`）
6. **MemoryKeeper（init）**：终稿 outline + 人设 → 初始化角色状态/伏笔表/时间线起点

> 生成单章流水线无此循环（线性：RAG→写→审→≤2次重写→记忆→RAG写→落库）。

---

## 4. CharacterDesigner 子提示词（已定稿）
- **来源**：主提示词 Step 2 #3 仅定义「主角/配角/反派人设卡（性格、动机、成长弧光）」；Coordinator 将其排在 WorldBuilder 之后。
- **现有代码版（stub）**：输出 `{characters:[{name, role, personality, motivation, current_status, growth_arc}]}`，`role ∈ {主角/配角/反派}`。
- **问答结论（2026-08-12）**：
  - 角色组成/数量：**由大纲驱动**——消费 PlotArchitect outline，按卷/章事件推导需要多少主角/配角/反派，不写死数量。
  - 字段深度：**基础6 + 结构化4 + 外貌/弱点/关系网/阵营**，即 `name/role/personality/motivation/current_status/growth_arc` + `level/mood/equipment/location` + `appearance/weakness/relationships/faction`（共 14 字段）。
  - 世界观耦合：**消费 WorldBuilder 的势力/种族**，`faction` 必须引用既有阵营/种族。
- **2026-08-12（三）补充**：用户确认「反派独立动机」必保留、并点出「大纲↔角色」循环依赖（大纲吃角色、角色吃大纲）。据此将 CharacterDesigner 明确置于两遍式对齐的中点（消费 Pass-1 骨架、被 Pass-2 反向消费），草稿已同步修订。
- **进度**：**已定稿 v1.0.0（2026-08-12）**，落盘 `子提示词 8：CharacterDesigner（角色设计师）.md` + 版本化 `prompts/character_designer.json`；随 Phase 2（与 Coordinator 两遍式）一起落地。

---

## 5. 进度日志（diary）
- **2026-08-12** —— 完成 7 子提示词 + 主提示词通读；产出差距分析与分阶段路线；确认 4 项设计决策（世界观落库可改 / 角色结构化 / 审校全自动 / CharacterDesigner 问答共创）；2.0 开发日记建账。
- **2026-08-12（续）** —— CharacterDesigner 问答完成：大纲驱动数量、14 字段人设卡、消费 WorldBuilder 势力/种族；子提示词草稿已落盘 `子提示词 8：CharacterDesigner（角色设计师）.md`，待用户确认。
- **2026-08-12（三）** —— 用户确认反派独立动机必保留，并指出「大纲↔角色」循环依赖。新增决策⑤（两遍式 PlotArchitect + CharacterDesigner 居中，由 Coordinator 编排，一次有界细化）；修订 CharacterDesigner 草稿与 §3.1 创建新书流水线；循环依赖已消解为可落地方案。
- **2026-08-12（四）** —— CharacterDesigner **定稿 v1.0.0**；并按用户决策把 8 个提示词（7 子提示词 + CharacterDesigner）全部版本化为 `prompts/*.json`（version/status/source_md/system_prompt/notes + `index.json` 索引），供代码加载与 git 版本管理；下一步从 Phase 0 开工（无阻塞项）。
- **2026-08-12（五）** —— **Phase 0 完成并通过集成测试（pytest 2 passed）**：① 删除死代码 `api/chapter.py`；② PlotArchitect 注入 title/genre/premise/style，system_prompt 升级为完整结构（total_outline + volumes[].arc + chapters[].development/ending_hook）；③ schemas 新增 TotalOutline、VolumeOutline.arc、ChapterOutline.development/ending_hook，OutlineGenerateResponse.total_outline；④ novel.py 存完整 dict 到 outline_json + 新增 `_volumes_from_outline` 归一化（generate-chapter 已用）；⑤ 前端 OutlineView 显示 arc 与章末钩子列。验证用隔离 venv `novelforge` + 桩 chromadb/zhipuai/asyncpg + `tests/test_phase0.py`。下一步 Phase 1 WorldBuilder。
- **2026-08-12（六~十二）** —— **Phase 1-6 全量落地并通过端到端测试（`pytest tests/` → 14 passed, 1 skipped）**：
  - **Phase 1 WorldBuilder**：`/generate-world` 落 `world_settings_json`（结构化 entries，可查看可手改 `PUT /world`）+ 写 RAG `world`；`tests/test_phase1.py` 验证 ingest_world_rag 被调用。
  - **Phase 2 CharacterDesigner**：14 字段人设卡写入 `characters` 表（含 level/mood/equipment/location/faction/appearance/weakness/relationships）；`/generate-characters` + `GET /characters`；`tests/test_phase2.py` 验证反派独立 motivation。
  - **Phase 3 ChapterWriter/ConflictEditor**：ChapterWriter 正文 + 末尾元数据行 `[字数|爽点|伏笔]` → 落库 meta；ConflictEditor 四维评分 + verdict + `{severity,location,fix}` 结构化 issues；`tests/test_phase3.py`。
  - **Phase 4 MemoryKeeper**：每章后 `/update-memory` 更新角色状态(level) + 埋设/回收伏笔(expected_resolve_chapter/resolved_chapter)；`tests/test_phase4.py` 验证林夜 炼气→筑基、伏笔可查。
  - **Phase 5 Coordinator（核心编排层）**：新建 `app/services/coordinator.py`，两条流水线——① `create_book_pipeline`：WorldBuilder→Pass-1 大纲骨架→CharacterDesigner→Pass-2 大纲细化→MemoryKeeper init（两遍式，Center 居中）；② `generate_chapter_pipeline`：RAG→ChapterWriter→ConflictEditor(<24 自动重写≤2次)→MemoryKeeper→落库。`novel.py` 的 `/bootstrap`、`/generate-chapter` 改走 Coordinator；新增 `GET /outline`（读已存不重生成）；`schemas/novel.py` 加 `BootstrapRequest/BootstrapResponse`。`tests/test_phase5.py` 验证两遍式与单章审校不重写/重写两条路径。
  - **Phase 6 体验层**：前端 `api.js` 加 `getOutline`/`bootstrap`；`stores/novel.js` 加 `loadOutline`/`bootstrap`（一键成书后拉 outline/characters/world/chapters）；DashboardView 加「✦ 一键成书」绿色按钮；ChapterView 加**打字机效果**（setInterval 16ms 渐进揭示 + 闪烁光标）。`retriever.retrieve` 加空语料防御（修 `BM25Okapi([])` 真实 bug）。`tests/test_phase6.py` 验证 `GET /outline` 成书后返回完整大纲。
  - **测试架构**：`tests/conftest.py` 统一 `test_shared.db` + 桩 zhipuai/asyncpg + autouse `reset_db`（`drop_all/create_all` + `engine.dispose()` 防跨事件循环污染）；全部 Phase 测试 patch 目标改 `coord_module.retrieve_for_generation`。
  - **本轮收尾修复**：`test_e2e.py` 导入期 `=` 覆盖全局 `DATABASE_URL=test_e2e.db`/`ZHIPU_API_KEY=""` 污染后续测试 → 改为移除覆盖、由 conftest 统一管理；其余 Phase 测试 `=`→`setdefault`；`memory_manager.py` 三处 `datetime.utcnow()`→`datetime.now(timezone.utc)`。组合跑由 2 failed 修复至 **14 passed, 1 skipped**（test_e2e 沙盒禁 sqlite 自动跳过），仅 2 个框架级告警（pytest-asyncio 标记未注册 / starlette TestClient httpx 弃用），无产品 bug。
  - **结论**：2.0 Coordinator 驱动的多 Agent 流水线（创建新书 + 生成单章两条）已端到端可玩，Phase 0-6 全部经测试验证。前端因本机 `node_modules` 不完整无法本地 `vite build`，部署仍以 Docker 为准（`docker compose up -d --build`）。

---

## 6. 下一步
- **Phase 0 现在可开工**（提示词已定稿 + 版本化，无阻塞项）：删 `api/chapter.py` 死代码 → 修 PlotArchitect 注入设定（注入 title/genre/premise/style，为两遍式打底）→ outline schema 补齐 `development`/`ending_hook`/`total_outline`。
- 提示词版本化已就绪：`prompts/*.json`（v1.0.0）+ `index.json`；各 Agent 接线时再补全 `input`/`output_schema` 结构化字段。

## 7. 提示词版本化（prompts/）
- 目录 `D:\AI-Novel-Forge\prompts\`：8 个 Agent 各一个 `{agent}.json` + `index.json` 索引。
- 每个 JSON 结构：`agent` / `display_name` / `version`(1.0.0) / `status`(final) / `updated_at` / `source_md`(原 .md 相对路径) / `system_prompt`(完整提示词文本) / `input`{} / `output_schema`{} / `notes`。
- 来源：用户「抖音ai夜校」作业 `提示词1.0/` 的 7 个子提示词 + 共创的 CharacterDesigner。`.md` 仍为人读源码，`prompts/*.json` 为代码加载与 git 版本管理的权威副本。
- 后续：每个 Agent 在其 Phase 接线时，把 `input`/`output_schema` 补全为结构化字段（如 ChapterWriter 元数据行、ConflictEditor 四维评分）；版本号按 SemVer 递增。
