# AI-Novel-Forge · 项目介绍与能力清单

> 定位：RAG + 多 Agent 协作的网文（爽文 / 玄幻 / 修仙 / 都市异能）自动创作系统。
> 本文聚焦**本会话升级的核心能力**，并补充项目运行方式，作为对外/对内介绍的底稿。

---

## 1. 技术栈

| 层 | 技术 |
|---|---|
| 后端 | FastAPI + SQLAlchemy(async **Core Table**，无 ORM 模型) + ChromaDB(本地向量) + PostgreSQL |
| 前端 | Vue3 `<script setup>` + Vite + Element Plus + Pinia |
| LLM | **OpenAI 兼容客户端**（智谱 GLM / DeepSeek / OpenAI / 通义 等可自由切换，靠 Base URL + Key + 模型名三字段适配） |
| 部署 | Docker Compose（postgres + backend + frontend） |

---

## 2. 本次升级的核心能力

### 2.1 章节编辑器：双栏 + 6 张辅助卡片
- 布局：左正文 `flex:1` + 右侧固定侧栏（紧贴正文右缘，右侧留白）。
- 6 张辅助卡片：**本章概要 / 伏笔追踪（可手动添加 + localStorage 持久化）/ 出场角色 / 角色关系网状图 / 章节笔记 / 快捷导航**。
- 卡片支持拖头部上下换序（flex `order` + `localStorage` 持久化），不自由乱跑。
- 正文全面加大字号/行高/字距，适配深色高对比阅读。

### 2.2 全局深色 / 浅色主题
- `App.vue` 侧边栏 🌙/☀️ 开关，统一 `localStorage['global_theme_dark']`，跨 tab 实时同步。
- 全站覆盖：正文、标题、编辑器、浮动面板、关系图弹窗、伏笔、笔记、导航。

### 2.3 角色关系 3D 力导向图谱（重点能力）
自研 Canvas 引擎，在 2D Canvas 上模拟 3D 纵深感：
- **视觉**：径向渐变球体 + 辉光 + Z 轴纵深（近大远小 / 近亮远暗）+ 深度排序渲染。
- **物理**：库仑斥力(12000) / 弹簧引力(理想长 180/240) / 中心引力 / 阻尼(0.72)，自动散开不堆叠。
- **交互**：
  - 🖱 拖动节点 → 松手**钉住不回弹**（金色 📌 标记），双击取消钉住；
  - 👆 点击节点 → 以该角色为中心重排关系网；
  - 🔄 打散重排 → 清除钉住 + 随机重布；
  - 🔍 滚轮缩放 → **整体缩放**（节点坐标 + 连线 + 文字一起放大，非只放大球）；
  - ✋ 空白区拖动 → **平移视图**（查看放大后超出视口的区域）；
  - 👁 隐藏节点 → 侧栏勾选隐藏角色简化视图（隐藏节点不渲染、不参与力模拟、不可点）；
  - 📖 操作手册内嵌于弹窗。
- 关系连线按 **盟友(绿) / 中立(白) / 敌对(红)** 着色，边标签加粗放大（15px / 700）。
- 全屏弹窗，画布动态自适应容器尺寸。

### 2.4 角色关系图谱「单一数据源」（消除重复）
- 之前 ChapterView 与 CharactersView 各持一份 ~400 行引擎（逐字相同，复制粘贴）。
- 现抽成 **`frontend/src/composables/useForceGraph.js`** 共享 composable，两页面 `import` 同一份引擎。
- 效果：**修一处，两处同时生效**，不再出现「同一功能两边表现不同」。

### 2.5 大纲页操作体验
- 操作列并排 `[写这章 / 重写这章] [查看]`，**未写章节的「查看」置灰**。
- 大纲 → 章节跳转（URL `?ch=N`，自动定位到该章并加载内容）。

### 2.6 模型设置面板（自由换模型）
- 底层改为 **OpenAI 兼容协议**（非仅智谱），靠 Base URL + Key + 模型名适配任意 OpenAI 格式服务。
- 弹窗内置**使用说明 + 获取 Key 直达链接**，以及 6 张**常用模型预设卡片**（智谱 / DeepSeek / OpenAI / 通义）一键填充。
- 全局统一设置（一处生效所有书），配置持久化到数据库（Key 脱敏返回）。

### 2.7 角色页内联编辑
- 卡片内「编辑 / 完成编辑」切换，字段（性格 / 动机 / 势力 / 关系 / 成长弧 …）变输入框，独立「保存此角色 / 还原」，顶部全局「保存全部」。

### 2.8 后台健壮性修复
- **创建新书 500**：`created_at` 时区混用（带时区 default × 裸 `DateTime`）→ 改为去时区。
- **一键成书 500**：智谱 SDK 的 `RateLimitError` import 路径错误（应为 `zhipuai.core._errors`）导致 429 重试整段失效 → 修正 import + 重试 4 次 + 友好 429 提示（非裸 500）。

---

## 3. 运行方式

```powershell
# 在 AI-Novel-Forge 根目录
docker compose -f deploy\docker-compose.yml up -d --build
```
- 前端：http://localhost:5173
- 后端文档：http://localhost:8000/docs
- 前端热更新需 `CHOKIDAR_USEPOLLING=true`（已在 compose 内配置）。
- ⚠️ 改动涉及**新增 Python 依赖（openai）**时，必须重建 backend 镜像：`docker compose -f deploy\docker-compose.yml up -d --build backend`。

---

## 4. 关键文件

| 文件 | 职责 |
|---|---|
| `frontend/src/composables/useForceGraph.js` | 角色关系 3D 力导向图谱引擎（共享） |
| `frontend/src/views/ChapterView.vue` | 章节编辑器（双栏 + 6 卡片 + 关系图） |
| `frontend/src/views/CharactersView.vue` | 角色管理（内联编辑 + 关系图谱） |
| `frontend/src/views/OutlineView.vue` | 大纲（操作列 + 跳转） |
| `frontend/src/App.vue` | 侧边栏导航 / 全局主题开关 / 模型设置 |
| `backend/app/core/llm_client.py` | OpenAI 兼容 LLM 客户端（429 重试） |
| `backend/app/core/runtime_config.py` | 模型设置单例 + DB 持久化 |

---

## 5. 已知技术债（2.0 待处理）
1. `backend/app/api/chapter.py` 是死代码（未注册、import 不存在），应删。
2. 大纲生成未把小说设定（title/genre/premise/style）注入模型，导致大纲与设定脱节。
3. WorldBuilder / CharacterDesigner / MemoryKeeper 三个 Agent 未接入路由，无对应 endpoint 真正落地。
4. 流式输出（`stream_generate`）已实现但未在前端落地（无打字机效果）。

> 更完整的架构 / API / 表结构基线见 `PROJECT_STATE.md`；开发日记见 `2.0_DEV_DIARY.md`。
